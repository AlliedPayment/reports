# reports

this is conventions repo/workflow/git.


This monorepo provides the following:

| package | top level shortnote |
|---------|-------|
|allied.SendEmail| lambda function to send templated emails with attachments and zip handling. |
|allied.TransformJq| lambda function to transform files via JSON configuration and jq. |

you are currently reading the [develop] branch; there is no readme on master...no release.
